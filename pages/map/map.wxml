<view class="page">
		<view class="mapBox">
		<map class="mapTU" id="map" longitude="{{longitude}}" latitude="{{latitude}}" polyline="{{polyline}}" markers="{{markers}}" enable-rotate show-location></map>
		<view></view>
		</view>
</view>